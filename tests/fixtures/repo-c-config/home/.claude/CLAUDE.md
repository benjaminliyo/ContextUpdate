# Personal global instructions (fixture mirror)

This file is the test stand-in for `~/.claude/CLAUDE.md`. The harness
points `$CU_HOME_CLAUDE_MD` at this path so tests never touch the real
user-global file.

## Style

Avoid classes. Prefer functions and closures.
